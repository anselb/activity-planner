$(document).ready(function(){

  $('.activity-add').click(function(){
      let mediaDiv = $(this).parent('.media-body').parent('.media')
    //   let name = mediaDiv.children('.media-body')
    // let itinId = new RegExp('/itineraries/' + id + '/activities').exec(window.location.href);
    // $.post('/itineraries/' + parseInt(itinId[0]) + '/activities', {name : $('#tip-content').val(), imageUrl : lat, activityUrl : lng, location : address}, function(){
    // });
  });


});
